services:
  backend:
    build: ./backend
    container_name: backend-container
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./client/url-shortener
    container_name: frontend-container
    ports:
      - "3000:3000"
    depends_on:
      - backend